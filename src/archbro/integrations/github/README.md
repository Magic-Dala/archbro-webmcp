# GitHub boundary

Place GitHub OAuth/API/webhook/repository adapters here. Verify provider input, convert GitHub-specific payloads into normalized external events, and hand them to the platform pipeline. GitHub handlers must not directly mutate Archbro Project, Living Architecture, or Task state.
