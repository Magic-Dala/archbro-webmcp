# Observability

Place application logging, metrics, traces, runtime health, and deployment telemetry here. Observability may consume product/runtime events but must not contain product decision logic or silently mutate project state.
