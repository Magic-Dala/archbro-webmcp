# Observability

Owner: Max.

Place application logging, metrics, traces, runtime health, and deployment telemetry here. Observability can consume product/runtime events but should not contain product decision logic or silently mutate project state.
